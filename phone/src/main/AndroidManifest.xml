<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:name="com.pipboywatch.PipBoyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">

        <!-- Launcher-visible companion screen: grant Health Connect access
             here once, then the watch pulls a fresh snapshot automatically
             every time its STAT tab opens.
             Fully-qualified names below: this module's namespace
             (com.pipboywatch.notes) only matches SendNoteActivity's actual
             package, so relative ".Foo" references would resolve wrong for
             anything in the newer com.pipboywatch.health / .sync / root
             packages — learned the hard way (see MainActivity not found). -->
        <activity
            android:name="com.pipboywatch.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Required by Health Connect: shown when the user taps the privacy
             policy link on the permission grant screen. -->
        <activity
            android:name="com.pipboywatch.health.PermissionsRationaleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>
        <activity-alias
            android:name="com.pipboywatch.health.ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity="com.pipboywatch.health.PermissionsRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <!-- Answers the watch's STAT_REQUEST_PATH pings with a Health
             Connect snapshot read on the phone. -->
        <service
            android:name="com.pipboywatch.sync.StatRequestListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
            </intent-filter>
        </service>

        <!-- Minimal Share-sheet receiver — not a full companion app.
             Accepts shared plain text from any phone app and relays it to
             the watch's DATA > Notes over the Wear Data Layer. -->
        <activity
            android:name="com.pipboywatch.notes.SendNoteActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="true"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>
</manifest>
