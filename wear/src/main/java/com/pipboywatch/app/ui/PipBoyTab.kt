package com.pipboywatch.app.ui

/** The five dial positions, in bezel rotation order — matches the real Pip-Boy's tab order. */
enum class PipBoyTab(val label: String) {
    STAT("STAT"),
    INV("INV"),
    DATA("DATA"),
    MAP("MAP"),
    RADIO("RADIO")
}
